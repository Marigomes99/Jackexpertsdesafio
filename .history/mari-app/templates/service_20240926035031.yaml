apiVersion: v1
kind: Service
metadata:
  name: mari-app
  labels:
    app: mari-app
    desafio: jackexperts  # Adicionando a label requerida
spec:
  type: LoadBalancer  # Mantendo como LoadBalancer
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: mari-app
